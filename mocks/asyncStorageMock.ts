// ✅ A fully typed AsyncStorage mock for web/Next.js

type  = <string, string>;

const storage:  = {};

const AsyncStorage = {
    storage[key] = ;
  },

  async getItem(key: string): Promise<string | null> {
    return storage[key] ?? null;
  },

  async re                         xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx]]]]]]]]]]]]]]]]m
  Item(key: string): Promise<void> {
    de[key];
  },

  async clear <void> {
    (Object.keys() as []).((key: sdelete [key]);
  },
};

;
